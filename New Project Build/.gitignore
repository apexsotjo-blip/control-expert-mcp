.venv/
__pycache__/
*.egg-info/
_e2e_work/
*.pyc
.pytest_cache/

# Installer build outputs and third-party tooling - regenerate, don't commit
installer/dist/
installer/build/
installer/tools/
installer/*.exe

# Control Expert project / archive binaries (regenerable test outputs)
*.stu
*.sta
*.zef
*.xef
*.ztx
*.bak
*.BAK

# Proprietary / customer data - NEVER commit
# (this repo used to be a subdirectory of control-expert-mcp, where these
# rules lived in the parent .gitignore; carried over verbatim on the split
# to its own repository so the protection isn't lost)
HMI_SAMPLES/
P24-IWT-03-SCADAPack_*/
RC_STU_PROBES/
remoteconnect.xls
RC_PROBE_import.xls
*_ImportLog.txt
pending.txt

# Local scratch/snapshot notes - never commit
_attic/
